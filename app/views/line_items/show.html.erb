<p id="notice"><%= notice %></p>

<p>
  <strong>Description:</strong>
  <%= @line_item.description %>
</p>

<p>
  <strong>Amount:</strong>
  <%= @line_item.amount %>
</p>

<%= link_to 'Edit', edit_invoice_line_item_path(invoice_id: @invoice.id, id: @line_item.id) %> |
<%= link_to 'Back', invoice_line_items_path(@invoice) %>
